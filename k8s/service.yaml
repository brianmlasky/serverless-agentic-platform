apiVersion: v1
kind: Service
metadata:
  name: litellm-gateway
  namespace: litellm
  labels:
    app: litellm
    cost-center: "research-dev"
spec:
  selector:
    app: litellm
  ports:
  - name: http
    port: 80
    targetPort: 4000
    protocol: TCP
  type: ClusterIP
