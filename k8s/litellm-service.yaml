apiVersion: v1
kind: Service
metadata:
  name: litellm-gateway
  namespace: litellm
  labels:
    app: litellm
    cost-center: research-dev
spec:
  type: ClusterIP
  selector:
    app: litellm-gateway
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 4000
